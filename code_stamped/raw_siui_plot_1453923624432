from pithy import *
import json 
from urllib import urlopen as uo

durr = []

while True:
    print [int(x) for x in uo("http://localhost:9000/data").read().strip().split(",")]

    #imshow(durr,aspect="auto")
    #showme()
    #clf()
    time.sleep(1)